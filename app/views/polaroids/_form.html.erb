<%= form_with(model: polaroid, local: true) do |form| %>
  <% if polaroid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(polaroid.errors.count, "error") %> prohibited this polaroid from being saved:</h2>

      <ul>
      <% polaroid.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :caption %>
    <%= form.text_field :caption, id: :polaroid_caption %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_area :image, id: :polaroid_image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
